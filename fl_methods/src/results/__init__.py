"""Slim, committed evaluation-input trees built from processed data.

``results/<Benchmark>/<Project>/<BugId>/`` holds everything the evaluation
needs for one bug (see :mod:`src.results.build` for the exact contents), so
``run_evaluation.py`` runs without any ``data/`` processed tree present.
"""
